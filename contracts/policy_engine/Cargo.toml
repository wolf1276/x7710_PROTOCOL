[package]
name = "policy_engine"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { version = "21.6.0" }

[dev-dependencies]
soroban-sdk = { version = "21.6.0", features = ["testutils"] }

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
